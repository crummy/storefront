{
  "version": "0.2.0",
  "configurations": [
   {
    "name": "Attach by Process Id",
    "type": "node",
    "request": "attach",
    "cwd": "${workspaceRoot}",
    "processId": "${command:PickProcess}"
   }
  ]
 }